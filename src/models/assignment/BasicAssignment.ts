/* In case we do not need to process the id of an assignment. */

export interface BasicAssignment {
    name: string,
    repository: string, //repository link of an assignment
    description: string
}